import { WcSafeIntake } from "@/components/wc-safe-intake";

export const metadata = { title: "Safer report for women and children" };

export default function SafeReportPage() {
  return <div className="page-wrap"><WcSafeIntake /></div>;
}
