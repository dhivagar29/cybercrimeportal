import { TakedownFlow } from "@/components/takedown-flow";

export const metadata = { title: "Stop the spread" };

export default function TakedownPage() {
  return <div className="page-wrap"><TakedownFlow /></div>;
}
